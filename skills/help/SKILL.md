---
name: help
description: Show available LCARS skills grouped by purpose
---

# LCARS Skills

Display the available skills to the user, grouped by purpose. No computation needed — just present this reference:

```
Observe
  /lcars:dashboard   — Rolling stats, drift history, correction fitness, active patterns
  /lcars:discover    — Scan environment for CLI tools, show registry status
  /lcars:deep-eval   — On-demand response evaluation against structured rubric
  /lcars:fmea        — Interactive failure mode analysis for response breakdowns
  /lcars:setup       — Validate installation and diagnose issues

Tune
  /lcars:calibrate   — Propose threshold adjustments from scoring evidence
  /lcars:consolidate — Trigger pattern consolidation from session data

Strategize
  /lcars:foundry     — Review and apply staged correction strategy proposals
```
